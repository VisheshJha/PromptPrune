# PromptPrune Extension - Installation Instructions

## Quick Installation Guide

### Step 1: Download the Extension
You should have received a zip file named `PromptPrune-Extension.zip` or a folder named `chrome-mv3-prod`.

### Step 2: Extract the Files
- **If you have a zip file**: Extract it to a folder (remember where you saved it!)
- **If you have a folder**: You're all set!

### Step 3: Install in Chrome

1. **Open Chrome** and navigate to:
   ```
   chrome://extensions/
   ```
   Or: Click the three dots (⋮) → Extensions → Manage Extensions

2. **Enable Developer Mode**
   - Toggle the switch in the top-right corner that says "Developer mode"

3. **Load the Extension**
   - Click the **"Load unpacked"** button
   - Navigate to and select the folder containing `manifest.json`
     - This is usually the `chrome-mv3-prod` folder (or whatever folder you extracted)

4. **Done!** 🎉
   - You should see the PromptPrune extension appear in your extensions list
   - The extension icon should appear in your Chrome toolbar

### Step 4: Pin the Extension (Optional)
- Click the puzzle piece icon (🧩) in Chrome's toolbar
- Find "PromptPrune" and click the pin icon (📌) to keep it visible

---

## For Other Browsers

### Microsoft Edge
Same process as Chrome! Edge uses the same extension format.

### Brave Browser
Same process as Chrome! Brave is Chromium-based.

### Firefox
1. Open Firefox and go to: `about:debugging`
2. Click "This Firefox" in the left sidebar
3. Click "Load Temporary Add-on"
4. Select the `manifest.json` file from the extension folder
5. **Note**: Firefox installation is temporary and will be removed when you restart Firefox

---

## Troubleshooting

### ❌ "Manifest file is missing or unreadable"
**Solution**: Make sure you selected the folder that contains `manifest.json`, not a parent folder.

### ❌ "This extension may have been corrupted"
**Solution**: 
- Make sure you extracted all files from the zip
- Try re-downloading the extension
- Contact the person who shared it with you

### ❌ Extension not appearing in toolbar
**Solution**:
- Check if it's in the extensions list (chrome://extensions/)
- Click the puzzle piece icon (🧩) to see all extensions
- Make sure the extension is enabled (toggle should be blue/on)

### ❌ "Developer mode extensions have been disabled"
**Solution**:
- This is a security feature in some organizations
- You may need administrator access to install extensions
- Try installing on a personal computer instead

---

## What the Extension Does

PromptPrune helps you:
- ✂️ **Optimize AI prompts** to reduce token usage
- 💰 **Save money** on AI API costs
- 📊 **Track token counts** across different AI models
- 🎯 **Apply prompt frameworks** (CoT, ToT, CREATE, etc.)
- 📈 **View savings history**

**All processing is done locally** - no data is sent to any servers!

---

## Privacy & Security

✅ **100% Local Processing**: All optimization happens on your computer
✅ **No Data Collection**: Nothing is sent to external servers
✅ **Open Source**: You can review the code if needed
✅ **Storage Only**: Uses Chrome's local storage for your optimization history

---

## Need Help?

If you encounter any issues:
1. Check the troubleshooting section above
2. Make sure you're using a recent version of Chrome/Edge/Brave
3. Contact the person who shared the extension with you

---

## Updates

Since this is a manually installed extension, it won't auto-update. When a new version is available:
1. You'll need to download the new version
2. Remove the old extension (chrome://extensions/ → Remove)
3. Install the new version following the same steps

---

**Enjoy using PromptPrune! 🌿**

