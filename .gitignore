node_modules
build
build 2
.cache
.env
*.log
.DS_Store
coverage
.vitest
.plasmo
*.zip
dist
# Extension private keys (NEVER commit these!)
extension_key*.pem
*_key_private.pem
*.pem

# Transformers assets SHOULD be committed (not ignored)
# They're small (~2-3MB) and needed for Local Asset Bridge
# !assets/transformers/